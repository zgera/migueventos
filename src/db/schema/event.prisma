enum EventCategory {
    CUMPLEAÑOS
    DEPORTE
    CONCIERTO
    CASUAL
    SOCIAL
    FIESTA
    OTRO
}

model Event {
    idEvent String @id @default(cuid())
    title String 
    date DateTime
    description String
    shortDescription String
    direction String
    creatorID String
    free Boolean
    category EventCategory @default(OTRO)
    assistants Int @default(0)
    cancelled Boolean @default(false)
    price Float?
    completed Boolean @default(false)
    imageURL String?
    users   Ticket[]
    participants TicketDetail[]
    user User @relation(fields: [creatorID], references: [idUser])
}
