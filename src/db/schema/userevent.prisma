model UserEvent {
    idUserEvent String @id @default(cuid())
    idUser String
    idEvent String
    admin Boolean 
    user User @relation(fields: [idUser], references: [idUser])
    event Event @relation(fields: [idEvent], references: [idEvent], onDelete: Cascade)
    createdAt DateTime @default(now())
}   